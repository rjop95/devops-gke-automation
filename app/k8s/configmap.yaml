apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-distro-config
  namespace: production
data:
  config_distro.py: |
    LOG_FILE = '/dev/null'
    CONSOLE_LOG_LEVEL = 20
    SCRIPT_NAME = '/pgadmin'
